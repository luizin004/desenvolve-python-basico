Início

    Escreva("Digite um número entre 1 e 9:")
    Leia(n)

    se n < 1 ou n > 9 então
        Escreva("Número inválido. Digite um número entre 1 e 9.")
    senão
        dezena ← n - 1
        unidade ← 10 - n
        resultado ← dezena * 10 + unidade

        Escreva("Resultado da multiplicação de 9 x", n, "é:", resultado)
    fimse

Fim
